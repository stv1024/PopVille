using UnityEngine;

/// <summary>
/// It is not an interface because only public fields can be initialized in the editor.
/// </summary>
public abstract class BaseHasAtlasFlags : MonoBehaviour
{
    //public AtlasManager.AtlasName UsedAtlasFlags;
}